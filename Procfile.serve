# backend: RAILS_ENV=production DEBUG=1 bin/rails s -p 3000 
# # https://github.com/mperham/sidekiq/issues/3090#issuecomment-389748695
# worker: RAILS_ENV=production RUBY_DEBUG_OPEN=0 dotenv bundle exec sidekiq -C config/sidekiq.yml

backend: DEBUG=1 bin/rails s -p 3000 

worker: RUBY_DEBUG_OPEN=0 dotenv bundle exec sidekiq -C config/sidekiq.yml

vite build --watch
